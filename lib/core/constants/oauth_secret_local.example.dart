/// Template for `oauth_secret_local.dart` (gitignored, not committed).
///
/// To set up a local build: copy this file to `oauth_secret_local.dart` in
/// this same directory, then replace the placeholder below with the real
/// client secret from Google Cloud Console → APIs & Services → Credentials
/// → the "Web application" OAuth client → "Client secret".
const String kGoogleWebClientSecret = 'PASTE_CLIENT_SECRET_HERE';
